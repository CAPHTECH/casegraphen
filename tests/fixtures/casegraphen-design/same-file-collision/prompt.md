# Fresh-context request

Two agents must edit `src/shared.rs` for different features. Use isolated worktrees but preserve any hidden resource dependency. Emit an unreviewed topology proposal and lint it.
